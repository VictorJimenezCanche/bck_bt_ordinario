name: Deploy Backend to GHCR and Render

on:
  push:
    branches:
      - main

permissions:
  contents: read 

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }} 

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GHCR (Using Repository Secret)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }} 
          password: ${{ secrets.GHCR_PAT }} 

      - name: Build and push images
        run: |
          # 1. Definir la etiqueta SHA
          SHA_TAG=${{ env.IMAGE_NAME }}:${{ github.sha }}
          # 2. Definir la etiqueta Latest (Monitoreada por Render)
          LATEST_TAG=${{ env.IMAGE_NAME }}:latest
          
          echo "Building image $LATEST_TAG..."
          # Construir con ambas etiquetas en un solo comando
          docker build -t $SHA_TAG -t $LATEST_TAG .
          
          # Push de ambas etiquetas simultáneamente
          echo "Pushing images..."
          docker push $SHA_TAG
          docker push $LATEST_TAG